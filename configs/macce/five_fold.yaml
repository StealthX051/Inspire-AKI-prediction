paths:
  artifacts_dir: /media/volume/ncs_inspire_data/ncs_aki/artifacts/macce_5fold_cv

study:
  cohort_key: strict_noncardiac_adult_procedure_audit
  outcome_key: macce

evaluation_mode: grouped_nested_cv

splits:
  use_bootstrapping: true
  n_bootstrap_iterations: 25
  n_cv_folds: 5

models:
  tabular_enabled:
    - log_reg
    - autogluon
    - xgb
    - rf
    - svm
    - mlp
    - knn
    - asa_rule
