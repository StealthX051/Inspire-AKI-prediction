paths:
  artifacts_dir: /media/volume/ncs_inspire_data/ncs_aki/artifacts/macce_default

study:
  cohort_key: strict_noncardiac_adult_procedure_audit
  outcome_key: macce

evaluation_mode: grouped_holdout

splits:
  use_bootstrapping: false

models:
  tabular_enabled:
    - log_reg
    - autogluon
    - xgb
    - rf
    - svm
    - mlp
    - knn
    - asa_rule
